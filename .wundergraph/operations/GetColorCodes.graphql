mutation($code: String!, $theme: String!, $language: String!){
    colorcodes_postColorCodes(postColorCodesInput: {code:$code,language:$language,theme:$theme}) {
      success
      data {
        content
        color
        lineNumber
      }
    }
}